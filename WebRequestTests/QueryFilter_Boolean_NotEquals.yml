info:
  name: QueryFilter_Boolean_NotEquals
  type: http
  seq: 30

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "NotEquals", "Operand": "IsEligibile", "Value": "True" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
