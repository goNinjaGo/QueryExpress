info:
  name: QuerySort_ByLastName_Descending
  type: http
  seq: 33

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "SortData": [ { "ColumnName": "LastName", "SortDirection": "Desc" } ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
