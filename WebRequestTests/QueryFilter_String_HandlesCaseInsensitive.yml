info:
  name: QueryFilter_String_HandlesCaseInsensitive
  type: http
  seq: 10

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "Contains", "IsCaseSensitive": false, "Operand": "FirstName", "Value": "jo" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
