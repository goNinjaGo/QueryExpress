info:
  name: QueryFilter_String_HandlesCaseInsensitive
  type: http
  seq: 10

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "FirstName", "Filters": [ { "Operation": "Contains", "Value": "jo", "IsCaseSensitive": false } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
