info:
  name: People
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "SortData": [
          {
            "ColumnName": "LastName",
            "SortDirection": "Asc"
          }
        ],
        "PageData": {
          "PageNum": 1,
          "PageSize": 10
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
