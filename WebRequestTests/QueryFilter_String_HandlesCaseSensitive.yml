info:
  name: QueryFilter_String_HandlesCaseSensitive
  type: http
  seq: 9

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "Contains", "IsCaseSensitive": true, "Operand": "FirstName", "Value": "Jo" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
