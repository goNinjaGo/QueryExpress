info:
  name: QueryFilter_String_HandlesCaseSensitive
  type: http
  seq: 9

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "FirstName", "Filters": [ { "Operation": "Contains", "Value": "Jo", "IsCaseSensitive": true } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
