info:
  name: QueryFilter_String_EndsWith
  type: http
  seq: 5

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "EndsWith", "Operand": "FirstName", "Value": "hn" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
