info:
  name: QueryFilter_Date_HandlesDifferentDataTypes
  type: http
  seq: 28

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "Equals", "Operand": "CreatedAt", "Value": "2021-01-01" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
