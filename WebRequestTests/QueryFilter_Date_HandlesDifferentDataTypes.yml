info:
  name: QueryFilter_Date_HandlesDifferentDataTypes
  type: http
  seq: 28

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "CreatedAt", "Filters": [ { "Operation": "Equals", "Value": "2021-01-01" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
