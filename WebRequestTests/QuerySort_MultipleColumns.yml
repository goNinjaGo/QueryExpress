info:
  name: QuerySort_MultipleColumns
  type: http
  seq: 34

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "SortData": [
          { "ColumnName": "IsEligibile", "SortDirection": "Desc" },
          { "ColumnName": "Age", "SortDirection": "Asc" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
