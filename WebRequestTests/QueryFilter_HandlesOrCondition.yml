info:
  name: QueryFilter_HandlesOrCondition
  type: http
  seq: 51

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "FirstName", "Operator": "Or", "Filters": [ { "Operation": "Equals", "Value": "John" }, { "Operation": "Equals", "Value": "Jane" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
