info:
  name: QueryFilter_NonSearchableColumn
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "ConfidentialData", "Filters": [ { "Operation": "Equals", "Value": "Test" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
