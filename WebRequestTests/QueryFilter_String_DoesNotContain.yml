info:
  name: QueryFilter_String_DoesNotContain
  type: http
  seq: 7

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "FirstName", "Filters": [ { "Operation": "DoesNotContain", "Value": "Jo" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
