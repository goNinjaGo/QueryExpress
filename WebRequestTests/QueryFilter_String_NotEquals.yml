info:
  name: QueryFilter_String_NotEquals
  type: http
  seq: 3

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "FirstName", "Filters": [ { "Operation": "NotEquals", "Value": "John" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
