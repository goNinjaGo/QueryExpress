info:
  name: QueryFilter_Numeric_Equals
  type: http
  seq: 11

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "Equals", "Operand": "Age", "Value": "30" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
