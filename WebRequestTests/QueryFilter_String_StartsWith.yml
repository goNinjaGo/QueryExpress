info:
  name: QueryFilter_String_StartsWith
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "StartsWith", "Operand": "FirstName", "Value": "J" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
