info:
  name: QueryFilter_Date_HandlesNullable
  type: http
  seq: 27

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "LessThan", "Operand": "UpdatedAt", "Value": "2023-01-01T00:00:00Z" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
