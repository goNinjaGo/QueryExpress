info:
  name: QueryFilter_HandlesMultipleColumns
  type: http
  seq: 50

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "IsUtilized", "Filters": [ { "Operation": "Equals", "Value": "True" } ] },
          { "Operand": "LastName", "Filters": [ { "Operation": "Equals", "Value": "Doe" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
