info:
  name: QueryFilter_Date_GreaterThan
  type: http
  seq: 25

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operand": "CreatedAt", "Filters": [ { "Operation": "GreaterThan", "Value": "2020-01-01" } ] }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
