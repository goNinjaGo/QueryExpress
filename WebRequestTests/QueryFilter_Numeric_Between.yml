info:
  name: QueryFilter_Numeric_Between
  type: http
  seq: 13

http:
  method: GET
  url: https://localhost:7233/api/Person
  body:
    type: json
    data: |-
      {
        "FilterData": [
          { "Operation": "Between", "Operand": "LitersUsed", "Value": "5", "SecondaryValue": "11" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
